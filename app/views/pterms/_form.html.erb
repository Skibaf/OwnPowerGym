<%= form_with(model: pterm) do |form| %>
  <% if pterm.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pterm.errors.count, "error") %> prohibited this pterm from being saved:</h2>

      <ul>
        <% pterm.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :pterm, style: "display: block" %>
    <%= form.text_field :pterm %>
  </div>
 
  <br>
  <div class="btn-group">
    <%= form.submit "Guardar", class: "btn btn-outline-dark" %>
  </div>
<% end %>
